# Shared 

**Owner**: Shared (Maintained collectively, but API contracts governed by Person 1)

## Boundaries
- Contains Data Transfer Objects (DTOs), types, and schema validations used by both frontend and backend.
- Enforces the shape of the canonical `SemesterOperationsState`.
- Any changes to DTOs must be reviewed against `interfaces/Interface.md` to ensure API compatibility.
