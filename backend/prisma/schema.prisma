// Phase 0: infrastructure only.
// Do NOT add product models here yet (users, semesters, subjects, etc).
// Those land in Phase 2 (Backend State Backbone) per Person_4_Backend_Infrastructure_SDD.md.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
