import React from 'react';
import { SpatialNotificationPortal } from './spatial/SpatialNotificationPortal';

export const NotificationCenterView: React.FC = () => {
  return <SpatialNotificationPortal />;
};
